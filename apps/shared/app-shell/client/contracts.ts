import type {
  GalleryApi,
  ServiceApiVersionInfo,
  ServiceCapabilities,
  ServiceHealthStatus,
} from '../types';

export type {
  ServiceApiVersionInfo,
  ServiceCapabilities,
  ServiceHealthStatus,
} from '../types';

export type GalleryClient = GalleryApi;
